apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'

allprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile('junit:junit:4.11')
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}
